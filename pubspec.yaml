name: barq
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  carousel_slider: ^4.2.1
  firebase_core: ^3.1.0
  cloud_firestore: ^5.0.0
  firebase_auth: ^5.0.0
  cupertino_icons: ^1.0.8
  google_sign_in: ^6.2.2
  provider: ^6.1.2
  google_maps_flutter: ^2.7.0
  geolocator: ^12.0.0
  geocoding: ^3.0.0
  firebase_messaging: ^15.0.0
  flutter_local_notifications: ^17.2.3  # <-- هاذ تحدث
  smooth_page_indicator: ^1.2.0+3
  intl: ^0.18.1
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
